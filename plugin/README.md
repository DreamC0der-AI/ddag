# ddag plugin for Claude Code

Installs the DDAG MCP server (`npx -y @dreamc0der/ddag`) for every project the session
opens. The chain lives at `./ddag.json` in the project folder; the dashboard
starts itself on http://localhost:5199/ the first time the server runs.

Full documentation: https://github.com/DreamC0der-AI/ddag
